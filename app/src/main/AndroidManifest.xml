<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 浮窗权限（安卓核心） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 长按振动提示（普通权限，安装即授予） -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".CakePetApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.CakePet">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CakePet.FullscreenTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".RandomSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".ThinkingSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".SnapSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".RangeSettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".MainThresholdActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".SnapThresholdActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".RangeThresholdActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".ThinkingThresholdActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".RandomThresholdActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <activity
            android:name=".TestHitActivity"
            android:exported="false"
            android:theme="@style/Theme.CakePet.FullscreenTitle" />

        <service
            android:name=".PetService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:ignore="ForegroundServiceType"
            tools:targetApi="34">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="cakepet_overlay" />
        </service>

        <!-- 无障碍服务：仅声明支持无障碍能力，需用户在系统设置中手动开启（不弹请求弹窗）。 -->
        <service
            android:name=".CakePetAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/accessibility_service_label">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
